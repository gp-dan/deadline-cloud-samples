
package:
  name: cinema4d-octane
  version: "2025"

source:
- path: ../../archive_files/cinema4d-octane-2025/win-64 # [win64]

build:
  number: 0
  # noarch: generic
  binary_relocation: false
  detect_binary_files_with_prefix: false
  ignore_prefix_files: true
  missing_dso_whitelist:
    - "**"

requirements:
  run:
    - cinema4d >=2025

about:
  license: LicenseRef-Multiple
